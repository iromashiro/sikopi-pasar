import './bootstrap';

import Alpine from 'alpinejs';

window.Alpine = Alpine;

Alpine.start();
// we just initialise Alpine (already via CDN for now)
// place extra JS if needed
console.log('SIKOPI PASAR frontend loaded');
